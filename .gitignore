# Ignore dependencies
node_modules/

# Ignore environment variables (private keys, secrets)
.env

# Ignore log files
*.log

# Ignore system files
.DS_Store
Thumbs.db

# Optional: npm cache and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: package lock if your team wants to regenerate it each time
# (usually keep this, so don’t ignore unless you’re told to)
# package-lock.json
